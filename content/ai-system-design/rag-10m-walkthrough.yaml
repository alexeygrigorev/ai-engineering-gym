id: rag-10m-walkthrough
category: ai-system-design
type: walkthrough
skill: ai-system-design
question: "Design a Document Q&A / RAG system for 10M+ articles."
difficulty: hard
status: draft
template_ref: sysdesign-rag-template
source_refs: [bhavishya-pandit, reddit-eightfold-ai, igotanoffer]

# My citations, researched for THIS walkthrough's reference answers.
sources:
  - title: "IGotAnOffer — Generative AI System Design Interview"
    url: "https://igotanoffer.com/en/advice/generative-ai-system-design-interview"
    supports: "clarify checklist, 5-step structure, deep-dive topics, failure modes and common mistakes"
  - title: "Chip Huyen — Building a Generative AI Platform"
    url: "https://huyenchip.com/2024/07/25/genai-platform.html"
    supports: "hybrid retrieval, guardrails, model gateway/router, prompt/exact/semantic caching, observability metrics (TTFT, context precision)"
  - title: "ByteByteGo — How Perplexity Built an AI Google"
    url: "https://blog.bytebytego.com/p/how-perplexity-built-an-ai-google"
    supports: "5-stage pipeline, dense+sparse+ML ranking, inline citations from retrieved context, small classifier routing simple vs complex queries, scale numbers"
  - title: "Slack Engineering — How We Built Slack AI to be Secure and Private"
    url: "https://slack.engineering/how-we-built-slack-ai-to-be-secure-and-private/"
    supports: "stateless RAG, RAG over fine-tuning for privacy, enforce requesting user's ACLs when fetching data to summarize/search"
  - title: "Pinecone — Rerankers and Two-Stage Retrieval"
    url: "https://www.pinecone.io/learn/series/rag/rerankers/"
    supports: "retrieve many with bi-encoder then cross-encoder rerank; LLM recall degrades as context grows so send fewer, better chunks"
  - title: "BigDataBoutique / Milvus — HNSW vs IVF vector index tradeoffs"
    url: "https://bigdataboutique.com/blog/hnsw-vs-ivfflat-how-to-choose-the-right-vector-index"
    supports: "brute force infeasible at millions of vectors; HNSW best recall/latency in RAM; IVF/IVF-PQ when index outgrows RAM"
  - title: "InterviewNode — GenAI System Design Interview Patterns"
    url: "https://www.interviewnode.com/post/generative-ai-system-design-interview-patterns-you-should-know"
    supports: "hallucination mitigation via grounding + confidence + citations; multi-layer caching and model tiering for cost"
  - title: "Uber — GenAI Gateway"
    url: "https://www.uber.com/blog/genai-gateway/"
    supports: "model gateway pattern: multi-vendor access, PII redaction, fallback, cost metrics and audit logs"

scenario: |
  Design a Document Q&A / RAG system over a corpus of 10M+ articles (think a large
  knowledge base or news archive). Users ask natural-language questions and expect
  accurate, cited answers. Assume ~50 QPS at peak, a p95 latency target of a few
  seconds, and that new/updated articles must become answerable within minutes.
  Answer each step OUT LOUD, then reveal the reference and self-rate.

steps:
  - phase: clarify
    prompt: "What do you ask before designing anything? Frame users, scale, freshness, quality bar, and guardrails."
    reference: |
      Don't jump to a solution — clarify first (the #1 interview mistake). Ask:
      (1) Users & use case — internal employees or public? factual lookup or open-ended
      synthesis? Is a cited, grounded answer required (yes for a knowledge base)?
      (2) Scale — 10M articles is roughly 10s of millions of chunks after chunking;
      ~50 QPS peak; how fast does the corpus grow? (3) Freshness — new articles
      answerable within minutes means near-real-time indexing, not a nightly batch.
      (4) Quality/NFRs — grounding/faithfulness bar, p95 latency (few seconds), and a
      cost-per-query budget. (5) Guardrails — access control (can every user see every
      article?), PII, prompt injection, and the behavior when nothing relevant is
      retrieved. State assumptions and confirm before drawing. [igotanoffer]
  - phase: high-level
    prompt: "Sketch the end-to-end architecture. Name both the offline and online paths and the cross-cutting layers."
    reference: |
      Two paths. OFFLINE ingest/index: article -> parse/clean -> chunk (with overlap)
      -> embed -> upsert into a vector index plus a metadata store (article id, ACL,
      timestamp, source URL for citations). ONLINE query/generate: query -> API
      gateway/auth -> input guardrails -> optional query rewrite -> embed -> hybrid ANN
      retrieve top-k -> rerank down to a few -> build a grounded prompt -> model
      gateway/router -> LLM generate -> output guardrails -> answer WITH inline
      citations. This mirrors Perplexity's five-stage pipeline (intent parse -> retrieve
      -> snippet extract -> generate-with-citations -> refine). Cross-cutting: model
      gateway, multi-layer cache, guardrails, and observability/eval. Keep it stateless
      RAG — pass all context per request so the model retains nothing (privacy + more
      relevant answers). [bytebytego-perplexity, slack, chip-huyen-platform]
  - phase: deep-dive
    prompt: "Chunking & embeddings: how do you split 10M articles and represent them for retrieval?"
    reference: |
      Chunk so each unit is a coherent, retrievable idea — semantic/paragraph-aware
      chunking with modest overlap beats naive fixed windows because it avoids cutting
      mid-thought and preserves citation granularity. Pick an embedding model matched to
      the domain and index those vectors; store rich metadata (article id, section, ACL,
      published/updated time, URL) alongside each chunk so you can filter, enforce
      permissions, and cite the exact source. Chunk size is a trade-off: smaller chunks
      = sharper retrieval but more chunks and more calls; larger chunks = more context
      per hit but noisier and more tokens. [igotanoffer, chip-huyen-platform]
  - phase: deep-dive
    prompt: "Retrieval at 10M+ vectors: which index and search strategy, and why not brute force?"
    reference: |
      Brute-force / flat search over tens of millions of vectors is too slow for a
      few-second p95 — you need approximate nearest neighbor (ANN). Default to HNSW: best
      recall-per-latency while the index fits in RAM. Once the index outgrows memory,
      move to IVF or IVF-PQ (cluster + product-quantize) to trade a little recall for a
      much smaller memory/cost footprint. Use HYBRID search: sparse/lexical (BM25) nails
      exact terms, rare identifiers, and names; dense/vector captures semantics — combine
      them (as Perplexity does with an ML ranking layer). Apply metadata filters and
      enforce the requesting user's ACLs at query time so retrieval never returns docs
      the user can't see. [bigdataboutique, chip-huyen-platform, bytebytego-perplexity, slack]
  - phase: deep-dive
    prompt: "Why add a reranker, and how does it change how many chunks you retrieve vs send to the LLM?"
    reference: |
      Two-stage retrieval. The bi-encoder ANN stage is fast but lossy (a whole chunk is
      squeezed into one vector), so retrieve MANY candidates (e.g. top 50-100) to
      maximize recall. Then a cross-encoder reranker reads each query-chunk pair jointly
      (less information loss) and reorders by true relevance, so you send only the top
      few (e.g. 3-8) to the LLM. This matters because LLM recall degrades as the context
      grows — maximize retrieval recall, then minimize tokens reaching the model. The
      cost is reranker latency, which is why it runs on a shortlist, not the whole
      corpus. [pinecone]
  - phase: deep-dive
    prompt: "Grounding, citations, and empty retrieval: how do you keep answers faithful and handle 'nothing relevant'?"
    reference: |
      Instruct the model to answer STRICTLY from the retrieved context and attach inline
      citations/provenance to each claim so users can verify — never let the LLM be the
      source of truth. When retrieval is empty or low-confidence (low similarity /
      sparse hits), don't hallucinate: abstain, say you don't know, or ask a clarifying
      question. Add an output-side faithfulness/hallucination check (e.g. an LLM judge or
      SelfCheckGPT-style verification) before returning. This is the core hallucination-
      mitigation pattern: retrieval grounding + confidence estimation + source
      transparency. [interviewnode, bytebytego-perplexity, chip-huyen-platform]
  - phase: deep-dive
    prompt: "How do you evaluate this system offline and online?"
    reference: |
      Offline: build a golden question/answer set with known source passages; run
      adversarial and red-team prompts; score RAG-specific metrics — context
      relevance/precision (did retrieval pull the right chunks?), groundedness/
      faithfulness (is the answer supported by context?), and answer quality — often with
      LLM-as-judge (acknowledge its bias/limits). Online: A/B tests plus feedback signals
      (thumbs up/down, click/citation-follow, escalations) feeding a continuous
      improvement loop. Segment metrics by prompt version, model, and time. Skipping an
      eval/monitoring plan reads as not production-ready. [igotanoffer, interviewnode, chip-huyen-platform]
  - phase: tradeoffs
    prompt: "How do you keep cost and latency in check at 50 QPS without hurting quality too much?"
    reference: |
      Bend the curve with caching and tiering rather than brute GPU spend. Caching:
      prompt cache for shared system-prompt/instruction prefixes; exact cache for
      repeated identical questions; semantic cache for near-duplicate queries (flag its
      false-match risk — a wrong similarity hit returns a wrong answer). Model tiering /
      routing: a small classifier sends easy queries to a cheap small model and only
      hard ones to a premium model (Perplexity routes this way). Stream tokens to improve
      perceived latency (TTFT), budget prompt tokens (rerank keeps context small), and
      parallelize independent steps. Track per-token cost and latency continuously via
      the model gateway. [chip-huyen-platform, interviewnode, bytebytego-perplexity, uber]
  - phase: tradeoffs
    prompt: "What breaks at scale, how do you detect it, and how does the system degrade gracefully?"
    reference: |
      Name failure modes AND detection: (1) Hallucination on empty retrieval — detect via
      groundedness eval + low-similarity thresholds; degrade by abstaining. (2) Stale or
      missing index — with minute-level freshness, a lagging ingestion pipeline serves old
      answers; monitor index-update lag and doc coverage. (3) Cost/latency blowup under
      traffic spikes — monitor per-query cost, QPS, p95, cache hit rate; degrade via
      throttling, smaller model, or shorter context. (4) Prompt injection / data leakage —
      malicious article or query manipulates the model; defend with input/output
      guardrails, ACL-scoped retrieval, and PII redaction at the gateway. (5) Context
      overflow — cap and rerank. Provider outage — fall back through the model gateway to
      an alternate model. Close with the biggest risks and what you'd improve next.
      [igotanoffer, chip-huyen-platform, uber, slack]

diagram: |
  graph LR
    subgraph Ingest [Ingest / Index - near-real-time]
      D[10M+ articles] --> P[Parse and clean]
      P --> C[Semantic chunk + overlap]
      C --> EM[Embedding model]
      EM --> IDX[(ANN vector index: HNSW / IVF-PQ)]
      C --> META[(Metadata + ACL + BM25 lexical index)]
    end
    subgraph Online [Query / Generate]
      U[User question] --> GW[API gateway / auth]
      GW --> IG[Input guardrails + PII redaction]
      IG --> QR[Query rewrite]
      QR --> QE[Embed query]
      QE --> RET[Hybrid retrieve top 50-100]
      META --> RET
      IDX --> RET
      RET --> RR[Cross-encoder rerank -> top 3-8]
      RR --> PB[Build grounded prompt]
      PB --> ROUTE{Query complexity router}
      ROUTE -->|simple| SMALL[Small / cheap model]
      ROUTE -->|complex| BIG[Premium model]
      SMALL --> OG[Output guardrails + faithfulness check]
      BIG --> OG
      OG --> A[Answer + inline citations]
    end
    CACHE[(Prompt / exact / semantic cache)] --- ROUTE
    OBS[Observability: context precision, groundedness, TTFT, cost, feedback] -.-> RET
    OBS -.-> OG
