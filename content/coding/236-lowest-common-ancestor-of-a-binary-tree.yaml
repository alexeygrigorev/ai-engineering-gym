id: leetcode-236-lowest-common-ancestor-of-a-binary-tree
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "236. Lowest Common Ancestor of a Binary Tree"
description: |
  Given the `root` of a binary tree (not necessarily a BST) and two nodes `p` and
  `q` present in it, return their lowest common ancestor: the deepest node having
  both `p` and `q` as descendants, where a node may be a descendant of itself.
  Example: in a tree rooted at 3, the LCA of `5` and `1` is `3`, and the LCA of `5`
  and `4` (with 4 under 5) is `5`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "Think recursively about which subtree(s) the two target nodes fall into."
  - "Recurse into both children, returning the node if it equals a target. Treat a node whose two children both report a find as the split point."
  - "Propagate whichever single non-null result bubbles up otherwise, giving O(n) time and O(h) space."
status: draft
answer:
  approach: >
    Recurse down the tree. A node is the LCA when p and q lie in different
    subtrees beneath it, or when the node equals p or q. Return the node when it equals p or q.
    Otherwise recurse into both children. If both sides return non-null, this
    node is the split point and thus the LCA. If only one side does, propagate
    that result upward.
  pseudocode: |
    lca(node):
        if node is null or node == p or node == q: return node
        left  = lca(node.left)
        right = lca(node.right)
        if left and right: return node        # p and q split here
        return left or right                  # whichever side found something
  complexity: { time: "O(n)", space: "O(h)" }
follow_ups:
  - "Solve it faster with BST ordering when the tree is a BST (LeetCode 235)."
  - "Adapt it to return null if either p or q is absent."
  - "Find the LCA using parent pointers instead of recursion."
sources:
  - { title: "LeetCode - Lowest Common Ancestor of a Binary Tree", url: "https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/", supports: "problem statement" }
  - { title: "Alexey's solution (236_common_ancestor.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/236_common_ancestor.cpp", supports: "reference implementation" }
