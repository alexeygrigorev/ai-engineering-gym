id: aieng-logistic-regression-numpy
category: coding
skill: ml-coding
type: coding
subtype: ml-coding
question: >
  Implement logistic regression from scratch in NumPy. Train it with stochastic
  gradient descent, add L2 regularization, and stop early when validation loss
  stops improving. Expose fit(X, y) and predict_proba(X).
difficulty: hard
status: draft
ai_relevant: true
source_refs: [datainterview-mistral]
answer:
  approach: >
    Logistic regression models P(y=1|x) = sigmoid(w·x + b). Fit by minimizing the
    average binary cross-entropy loss plus an L2 penalty (lambda/2)*||w||^2 (don't regularize the bias). The gradient of cross-entropy is beautifully simple. For a single example, dL/dz = (sigmoid(z) - y), so the weight gradient is (p - y)*x + lambda*w and the bias gradient is (p - y). SGD updates the weights after each example (or small minibatch), shuffling each epoch. Early stopping holds out a validation split and tracks its loss each epoch. It stops when the loss fails to improve for `patience` epochs, restoring the best weights seen. Two numerical details matter: use a numerically stable sigmoid (branch on the sign of z, or use log1p) to avoid exp overflow, and standardize features so the single learning rate works across dimensions.
  pseudocode: |
    def sigmoid(z):                      # stable: avoid exp(+large)
        return where(z >= 0, 1/(1+exp(-z)), exp(z)/(1+exp(z)))

    def fit(X, y, lr, lam, epochs, patience):
        w = zeros(n_features); b = 0
        best_w, best_loss, bad = w, inf, 0
        Xtr, ytr, Xval, yval = split(X, y)
        for epoch in range(epochs):
            for i in shuffle(range(len(Xtr))):        # SGD: one example at a time
                p = sigmoid(Xtr[i] @ w + b)
                grad = p - ytr[i]                     # dL/dz
                w -= lr * (grad * Xtr[i] + lam * w)   # L2 on weights only
                b -= lr * grad
            vloss = bce(sigmoid(Xval @ w + b), yval) + (lam/2)*w@w
            if vloss < best_loss - 1e-6:
                best_loss, best_w, best_b, bad = vloss, w.copy(), b, 0
            else:
                bad += 1
                if bad >= patience: break             # early stopping
        return best_w, best_b

    def predict_proba(X): return sigmoid(X @ w + b)
  complexity:
    time: "O(epochs * n_samples * n_features) for training; O(n_features) per prediction"
    space: "O(n_features) for the weight vector (plus the data)"
hints:
  - "The gradient of binary cross-entropy w.r.t. the pre-activation z is just (p - y), which gives you both the weight and bias updates."
  - "SGD means update per example (shuffle each epoch), not per full pass. L2 adds a lambda*w term to the weight gradient only. Never regularize the bias."
  - "Early stopping needs a validation split, so track val loss each epoch, keep the best weights, and stop after `patience` epochs with no improvement. Guard sigmoid against exp overflow."
follow_ups:
  - "How would you switch from pure SGD to minibatch, and what's the trade-off?"
  - "How do you extend this to multiclass (softmax regression)?"
  - "Where would L1 change the update, and why can't you just take its plain gradient at zero?"
  - "How would you add a learning-rate schedule or momentum, and when does it help?"
  - "How do you validate your gradient is correct (gradient checking)?"
sources:
  - key: mlmastery
    title: "Machine Learning Mastery — Logistic Regression with SGD from scratch in Python"
    url: "https://machinelearningmastery.com/implement-logistic-regression-stochastic-gradient-descent-scratch-python/"
    supports: "SGD update rule for logistic regression, per-example weight updates"
  - key: tds-l2
    title: "Towards Data Science — Implement Logistic Regression with L2 Regularization from Scratch"
    url: "https://towardsdatascience.com/implement-logistic-regression-with-l2-regularization-from-scratch-in-python-20bd4ee88a59"
    supports: "L2 penalty term added to the cost and gradient"
  - key: datainterview-mistral
    title: "DataInterview — Mistral Machine Learning Engineer Interview"
    url: "https://www.datainterview.com/blog/mistral-machine-learning-engineer-interview"
    supports: "reported task: implement logistic regression with SGD, L2, and early stopping in NumPy"
