id: leetcode-055-jump-game
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "55. Jump Game"
description: |
  Given an array `nums` where `nums[i]` is the maximum forward jump length from index `i`,
  determine whether you can reach the last index starting from index `0`. Return `true` or
  `false`. Example: `nums = [2,3,1,1,4]` -> `true`; `nums = [3,2,1,0,4]` -> `false`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "You don't need to track every possible jump sequence, just the farthest index reachable so far."
  - "Scan the array left to right, updating the farthest reachable index at each step. Do this only while the current index is still within that reachable range."
  - "This greedy single pass tells you in O(n) time and O(1) space whether the last index can ever be reached."
status: draft
answer:
  approach: >
    Greedy single pass tracking the farthest index reachable so far. Scan left to right,
    but only through positions you can actually reach (i <= max_index). At each i update
    max_index = max(max_index, i + nums[i]). If you can reach the last index, return true.
    If the loop stalls because i outran max_index, return false.
  pseudocode: |
    max_index = 0
    for i in 0..n-1:
        if i > max_index: return false      # can't reach here
        max_index = max(max_index, i + nums[i])
    return max_index >= n - 1
  complexity: { time: "O(n)", space: "O(1)" }
sources:
  - { title: "LeetCode — Jump Game", url: "https://leetcode.com/problems/jump-game/", supports: "problem statement" }
  - { title: "Alexey's solution (055_jump_game.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/055_jump_game.cpp", supports: "reference implementation" }
