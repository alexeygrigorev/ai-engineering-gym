id: leetcode-115-distinct-subsequences
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "115. Distinct Subsequences"
description: |
  Given two strings `s` and `t`, return the number of distinct subsequences of `s`
  that equal `t`. A subsequence keeps the original order but may drop characters.
  Example: `s = "rabbbit"`, `t = "rabbit"` -> `3` (three different ways to pick the
  matching characters).
difficulty: hard
tags: [dsa]
ai_relevant: false
hints:
  - "Think about counting matches between prefixes of the source string and prefixes of the target rather than the whole strings."
  - "Build a table where each cell adds the ways that skip the current source character. When characters match, it also adds the ways that use it to complete the target."
  - "Filling this 2D table bottom-up gives an O(n*m) time and space solution."
status: draft
answer:
  approach: >
    Count how many distinct subsequences of s equal t. Fill a table dp[i][j]
    holding the number of times t[:i] occurs in s[:j]. The first row is all ones
    (an empty target is always matched once) and the first column below it is
    zero. If the characters differ, s[j] can't help, so dp[i][j] = dp[i][j-1]
    (same count as without s[j]). If they match, dp[i][j] = dp[i][j-1] +
    dp[i-1][j-1]: the ways that ignore s[j] plus the ways where s[j] completes
    the current target character.
  pseudocode: |
    dp[0][*] = 1                 # empty t matched once
    dp[i>0][0] = 0               # empty s can't contain non-empty t
    for i in 1..len(t):
        for j in 1..len(s):
            if t[i-1] == s[j-1]:
                dp[i][j] = dp[i][j-1] + dp[i-1][j-1]
            else:
                dp[i][j] = dp[i][j-1]
    return dp[len(t)][len(s)]
  complexity: { time: "O(n*m)", space: "O(n*m)" }
follow_ups:
  - "Reduce to O(m) memory with a rolling row over s."
  - "Explain the match case dp[i][j-1] + dp[i-1][j-1] with a small example."
  - "How would counts overflow, and how do you guard against it?"
sources:
  - { title: "LeetCode - Distinct Subsequences", url: "https://leetcode.com/problems/distinct-subsequences/", supports: "problem statement" }
  - { title: "Alexey's solution (115_distinct_subsequences.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/115_distinct_subsequences.cpp", supports: "reference implementation" }
