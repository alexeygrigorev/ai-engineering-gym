id: leetcode-207-course-schedule
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "207. Course Schedule"
description: |
  You must take `numCourses` courses labeled `0` to `numCourses - 1`. You are given
  a list `prerequisites` where `[a, b]` means you must take course `b` before course
  `a`. Return `true` if it is possible to finish all courses, `false` otherwise
  (i.e., whether the dependency graph is free of cycles). Example:
  `numCourses = 2`, `prerequisites = [[1,0],[0,1]]` -> `false`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "Model prerequisites as a directed graph and ask whether that graph contains a cycle."
  - "Run DFS marking nodes as currently being visited versus fully finished. Reaching a node still marked as being visited means you found a back edge."
  - "Detecting any such cycle runs in O(V + E) time and means the schedule is infeasible."
status: draft
answer:
  approach: >
    Courses and their prerequisites form a directed graph. The schedule is
    feasible if and only if the graph has no cycle. Run DFS marking each node
    VISITING when entered and VISITED when finished. Reaching a node still
    marked VISITING means a back edge and thus a cycle, so it's infeasible.
    Translating the recursion to an explicit stack means pushing each node
    twice. Push once to open it (mark VISITING), and once with a counter to
    close it (mark VISITED).
  pseudocode: |
    build adjacency list: edge prereq -> course
    for each unvisited node: if not DFS(node): return false

    DFS(start):
        stack = [(start, 0)]
        while stack:
            (n, cnt) = pop
            if cnt == 0 and status[n] == VISITING: return false     # cycle
            if cnt > 0: status[n] = VISITED; continue
            status[n] = VISITING; push (n, 1)
            for m in graph[n]: if status[m] != VISITED: push (m, 0)
        return true
  complexity: { time: "O(V + E)", space: "O(V + E)" }
follow_ups:
  - "Return a valid course ordering, not just yes/no (LeetCode 210)."
  - "Solve it with Kahn's algorithm using in-degrees and a queue."
  - "How does the two-push counter emulate the recursive call stack?"
sources:
  - { title: "LeetCode - Course Schedule", url: "https://leetcode.com/problems/course-schedule/", supports: "problem statement" }
  - { title: "Alexey's solution (207_course_schedule.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/207_course_schedule.cpp", supports: "reference implementation" }
