id: leetcode-068-text-justification
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "68. Text Justification"
difficulty: hard
tags: [dsa]
ai_relevant: false
hints:
  - "Split the problem into two separate steps: first decide which words belong on each line, then decide how to space the words on each line."
  - "Greedily pack as many words as fit within max_width (counting single spaces) to form each line, then distribute leftover spaces as evenly as possible across the gaps for non-last lines, giving earlier gaps any extra space; the last line and single-word lines are left-justified with trailing padding."
  - "This two-phase greedy packing plus careful gap arithmetic processes the text in O(total characters) time and space."
status: draft
answer:
  approach: >
    A greedy line-packing problem followed by careful spacing. First greedily group words:
    keep adding the next word to the current line while it still fits in max_width (counting
    one space between words); when it doesn't fit, close the line and start a new group. Then
    render each group: for a normal (non-last) line with multiple words, distribute the
    leftover spaces as evenly as possible across the gaps, giving the earlier gaps the extra
    space when it doesn't divide evenly; a single-word line (and the last line) is
    left-justified and padded with trailing spaces to max_width.
  pseudocode: |
    # group
    groups = greedily pack words so each fits in max_width (with single spaces)
    # render
    for each group except the last:
        if one word: left-justify + pad trailing spaces
        else:
            gaps = words - 1
            base, extra = divmod(max_width - total_word_len, gaps)
            give each gap `base` spaces, first `extra` gaps one more
    last line: join with single spaces, pad trailing to max_width
  complexity: { time: "O(total characters)", space: "O(total characters)" }
sources:
  - { title: "LeetCode — Text Justification", url: "https://leetcode.com/problems/text-justification/", supports: "problem statement" }
  - { title: "Alexey's solution (068_text_justification.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/068_text_justification.cpp", supports: "reference implementation" }
