id: leetcode-204-count-primes
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "204. Count Primes"
description: |
  Given an integer `n`, return the number of prime numbers that are strictly less
  than `n`. A prime is an integer greater than 1 with no positive divisors other
  than 1 and itself. Example: `n = 10` -> `4` (the primes below 10 are 2, 3, 5, 7).
difficulty: medium
tags: [dsa]
ai_relevant: true
hints:
  - "Instead of testing each number for primality individually, mark composite numbers in bulk."
  - "Use the Sieve of Eratosthenes: walk upward, and whenever a number is still marked prime, cross out all of its multiples as composite."
  - "Striking multiples rather than checking each number individually gives O(n log log n) time."
status: draft
answer:
  approach: >
    Count primes below n with the Sieve of Eratosthenes, marking all numbers
    prime initially. Walking the odd numbers upward, count each one that's still
    marked prime and strike out all of its multiples as composite. Striking
    multiples is far cheaper than testing each number individually for
    primality.
  pseudocode: |
    is_prime[2..n-1] = true
    count = 1                       # account for 2
    for i = 3, 5, 7, ... < n:
        if not is_prime[i]: continue
        count += 1
        for j = 2i, 3i, ... < n: is_prime[j] = false
    return count
  complexity: { time: "O(n log log n)", space: "O(n)" }
follow_ups:
  - "Why can you start striking multiples from i*i rather than 2*i?"
  - "Reduce memory with a segmented sieve."
  - "Compare against trial-division primality testing per number."
sources:
  - { title: "LeetCode - Count Primes", url: "https://leetcode.com/problems/count-primes/", supports: "problem statement" }
  - { title: "Alexey's solution (204_count_primes.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/204_count_primes.cpp", supports: "reference implementation" }
