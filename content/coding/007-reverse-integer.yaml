id: leetcode-007-reverse-integer
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "7. Reverse Integer"
description: |
  Given a signed 32-bit integer `x`, return `x` with its digits reversed, keeping the
  sign. If the reversed value falls outside the 32-bit signed range
  `[-2^31, 2^31 - 1]`, return `0`. Example: `x = 123` -> `321`; `x = -123` -> `-321`;
  `x = 120` -> `21`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "Work on the absolute value and peel off digits one at a time using mod and integer division by 10."
  - "Rebuild the reversed number by repeatedly multiplying the accumulator by 10 and adding the next digit, then restore the original sign at the end."
  - "Guard against 32-bit overflow by checking after each step that dividing the accumulator back by the same power of ten still returns the digit you added. Return 0 if not."
status: draft
answer:
  approach: >
    Pull digits off the number one at a time with mod/divide by 10. Rebuild the
    reversed value by multiplying the accumulator by 10 and adding each digit. Handle
    the sign separately by working on the absolute value. The one subtlety is 32-bit
    overflow: after each step check that dividing the accumulator back by the current
    power of ten still yields the digit you added. If it doesn't, the value overflowed,
    so return 0.
  pseudocode: |
    sign = sign(x); x = abs(x)
    digits = []
    while x != 0: digits.push(x % 10); x /= 10
    result = 0; pow = 1
    for d in digits (most significant first):
        result += pow * d
        if result / pow != d: return 0     # overflow
        pow *= 10
    return sign * result
  complexity: { time: "O(log10(x)) — number of digits", space: "O(log10(x))" }
sources:
  - { title: "LeetCode — Reverse Integer", url: "https://leetcode.com/problems/reverse-integer/", supports: "problem statement" }
  - { title: "Alexey's solution (007_reverse_digits.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/007_reverse_digits.cpp", supports: "reference implementation" }
