id: leetcode-089-gray-code
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "89. Gray Code"
description: |
  Given an integer `n`, return any valid `n`-bit Gray code sequence: a list of the
  `2^n` integers from `0` to `2^n - 1` where every consecutive pair (and the first
  and last) differ in exactly one bit. The sequence must start with `0`.
  Example: `n = 2` -> `[0, 1, 3, 2]`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "Reflecting an existing list of codes and adding a new high bit preserves the property that consecutive codes differ by one bit."
  - "Build the sequence bit by bit, appending the current list in reverse order with the new bit set on each copy."
  - "This doubles the list every round, producing all 2^n codes in O(2^n) time."
status: draft
answer:
  approach: >
    Build the sequence iteratively by reflection, starting with [0]. For each
    bit i from 0 to n-1, take the current list. Walking it in reverse order, set
    bit i on each existing code and append the result. Reflecting the list and
    prefixing the new high bit guarantees consecutive codes differ by exactly
    one bit. The list doubles each round until it holds all 2^n codes.
  pseudocode: |
    result = [0]
    for i in 0..n-1:
        for k from len(result)-1 down to 0:
            result.append(result[k] | (1 << i))
    return result
  complexity: { time: "O(2^n)", space: "O(2^n)" }
follow_ups:
  - "Prove that each adjacent pair differs by exactly one bit."
  - "Produce the i-th Gray code directly with the formula g = i XOR (i >> 1)."
  - "Convert a Gray code back to its binary index."
sources:
  - { title: "LeetCode - Gray Code", url: "https://leetcode.com/problems/gray-code/", supports: "problem statement" }
  - { title: "Alexey's solution (089_gray_code.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/089_gray_code.cpp", supports: "reference implementation" }
