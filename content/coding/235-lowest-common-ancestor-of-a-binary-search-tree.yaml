id: leetcode-235-lowest-common-ancestor-of-a-binary-search-tree
category: coding
skill: algorithms
type: coding
subtype: algorithm
question: "235. Lowest Common Ancestor of a Binary Search Tree"
description: |
  Given the `root` of a binary search tree and two nodes `p` and `q` present in it,
  return their lowest common ancestor: the deepest node that has both `p` and `q` as
  descendants (a node may be a descendant of itself). Example: in a BST rooted at 6,
  the LCA of `2` and `8` is `6`, and the LCA of `2` and `4` is `2`.
difficulty: medium
tags: [dsa]
ai_relevant: false
hints:
  - "BST ordering tells you which subtree both target nodes must lie in at each step."
  - "Starting at the root, move left if both values are smaller and right if both are larger. Stop where they diverge or one matches the current node."
  - "That first split point gives the LCA in O(h) time and O(1) space."
status: draft
answer:
  approach: >
    Use the BST ordering, walking down from the root. Go left if both p and q
    are smaller than the current node, and go right if both are larger. The
    first node where they split is the lowest common ancestor, as is any node
    that equals p or q. The implementation traces the root-to-p and root-to-q
    paths and returns their last common node.
  pseudocode: |
    node = root
    loop:
        if p.val < node.val and q.val < node.val: node = node.left
        elif p.val > node.val and q.val > node.val: node = node.right
        else: return node        # split point, or node equals p or q
  complexity: { time: "O(h)", space: "O(1)" }
follow_ups:
  - "Solve the general (non-BST) tree version (LeetCode 236)."
  - "Why is the first split point guaranteed to be the LCA?"
  - "Handle the case where one node is an ancestor of the other."
sources:
  - { title: "LeetCode - Lowest Common Ancestor of a Binary Search Tree", url: "https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/", supports: "problem statement" }
  - { title: "Alexey's solution (235_common_ancestor_bst.cpp)", url: "https://github.com/alexeygrigorev/leetcode-solutions/blob/master/leetcode/src/235_common_ancestor_bst.cpp", supports: "reference implementation" }
